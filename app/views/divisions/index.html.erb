<h1>Divisions</h1>

<table align = "center", width="60%">
  <tr >
    <th>Title</th>
    <th>Logo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @divisions.each do |division| %>
  <tr align = "center">
    <td><%= division.title %> :</td>
    <td><%= link_to image_tag(division.photo, :size=>"100x100", :alt=>"Click to See Details"), division %></td>
  </tr>
  

<% end %>
</table>
<br />
<%= link_to 'Back Home', pages_home_path(@pages) %>
<br />

