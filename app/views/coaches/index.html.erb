<h1>Coaches</h1>

<table id="align" width="60%">
  <tr >
    <th>Team</th>
    <th>Name</th>
    <th>Nationality</th>
    <th>Yearsexperience</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @coaches.each do |coach| %>
  <tr align = "center">
    <td><%= coach.team.name %></td>
    <td><%= coach.name %></td>
    <td><%= coach.nationality %></td>
    <td><%= coach.yearsExperience %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Back Home', pages_home_path(@pages) %>